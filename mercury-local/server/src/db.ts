// Prisma client for the LOCAL sqlite DB. Generated into server/src/generated/prisma
// (gitignored). Import './env' FIRST so DATABASE_URL is set before the client reads it.
import './env.js';
import { PrismaClient } from '@prisma/client';

export const prisma = new PrismaClient();
