# Mercury (procurement / reorder board) build-time env. Baked into the static bundle at
# `npm run build`, so the Railway→custom-domain cutover is an env edit + redeploy, not code.

# The shared Minerva api that serves /api/auth/login and /api/mercury/*.
# Phase 1: the Minerva api's Railway URL. Phase 2: https://api.prominentdental.com
VITE_API_URL=http://localhost:3000
